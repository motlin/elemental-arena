{
	"$schema": "https://raw.githubusercontent.com/fallow-rs/fallow/main/schema.json",
	// src/**/*.mdx mirrors the Storybook stories glob in .storybook/main.ts;
	// fallow's storybook plugin only detects *.stories.* files, not docs pages.
	"entry": ["src/main.{ts,tsx,js,jsx}", "src/**/*.mdx"],
	"ignorePatterns": [".storybook/**"],
	// Tooling that never appears in an import: Storybook addons and the test
	// runner are named as strings in .storybook/main.ts, eslint-plugin-storybook
	// and jiti are loaded by vite.config.ts, and @storybook/addon-docs is only
	// imported from .mdx docs pages, which fallow reads as production source.
	// lucide-react and @tanstack/react-query-devtools are genuinely unused and
	// should come off this list by being uninstalled, not ignored.
	"ignoreDependencies": [
		"@chromatic-com/storybook",
		"@sentry/vite-plugin",
		"@storybook/addon-a11y",
		"@storybook/addon-docs",
		"@storybook/addon-onboarding",
		"@storybook/addon-vitest",
		"@storybook/test-runner",
		"@tanstack/react-query-devtools",
		"eslint-plugin-storybook",
		"jiti",
		"lucide-react",
	],
	// The game's modules export their rules and their panels so the jsdom tests can drive them
	// through src/game/game.ts, and fallow cannot see that through the `await import()` the
	// harness loads it with. The data tables under src/game/data are imported normally and stay
	// under the usual checks.
	"ignoreExports": [{"file": "src/game/*.ts", "exports": ["*"]}],
	"rules": {
		"unused-dependencies": "warn",
		// The rules modules call each other the way the game reads: a step lands on ground that
		// hurts, a fall ends a turn, an ended turn moves everyone. They are one layer and they
		// point both ways. Everything above them -- menu, render, input, board -- is one-way, and
		// nothing below them points back up: src/game/view.ts is the seam that keeps it that way.
		"circular-dependencies": "warn",
	},
}
